import FormHeader from '@components/Common/FormHeader'

export default {
  title: 'Component/Common/FormHeader',
  component: FormHeader,
}

export const Default = (args: any) => {
  return <FormHeader {...args} />
}
