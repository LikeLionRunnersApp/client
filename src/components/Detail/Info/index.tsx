import styled from "@emotion/styled";
import DetailInfo from "./DetailInfo";

const Info = () => {
  return (
    <div>
      <DetailInfo />
      <p>어쩌구 저쩌구 같이해요~</p>
    </div>
  );
};

export default Info;
